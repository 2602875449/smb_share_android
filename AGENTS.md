# AGENTS 规则（smb_share_android）

> 本文档约定本仓库中人类开发者和 AI 助手（“Agent”）协作时需要遵守的规则。

## 1. 目标

- 保持代码风格统一、可维护。
- 确保浅色 / 深色主题下的视觉一致性。
- 避免破坏现有核心功能（SMB 连接、文件列表、下载历史等）。

---

## 2. 一般开发规则

- 使用 Kotlin 官方推荐代码风格：4 空格缩进、驼峰命名、不可变优先。
- 新增业务逻辑优先放在 ViewModel / data 层，而不是直接写在 Composable 中。
- 核心逻辑需有简短中文注释，说明“为什么这样做”，而不是只描述“做了什么”。

---

## 3. UI & 主题颜色规范

1. **颜色使用**
   - 禁止在 UI 组件中直接写 `Color(0xFFxxxxxx)` 等硬编码颜色。
   - 必须通过统一主题获取颜色，例如：
     - `MaterialTheme.colorScheme.primary / secondary / background / onSurface` 等。
   - 新增颜色如确有需要，应先在主题层集中定义，再在组件中引用。

2. **主色 / 状态色**
   - 主色用于：底部导航选中状态、主要按钮、关键图标/高亮。
   - 危险操作（删除、覆盖等）统一使用“危险色”（Destructive 风格），避免随意使用纯红色。
   - 禁用态通过调整透明度（如 `copy(alpha = 0.38f)`）实现，而不是随意换一种完全不相关的颜色。

3. **浅色 / 深色主题适配**
   - 不允许只在单一主题下看起来“刚好好看”，必须确认在浅色、深色下都可用。
   - 不直接在组件里根据 `isSystemInDarkTheme()` 写两套颜色分支，优先交给主题来处理。

4. **间距与圆角**
   - 统一使用 **8 的倍数** 作为基础间距（如 `8.dp / 12.dp / 16.dp / 24.dp` 等）。
   - 圆角常用值建议为 `4.dp / 8.dp / 12.dp / 16.dp`，避免出现 7dp、13dp 等随意值。
   - 底部导航等高频组件高度保持在 64–72dp 区间，避免过高或过低导致手感异常。

5. **文字样式**
   - 文本样式优先使用 `MaterialTheme.typography` 中已有的层级（如 `titleMedium`, `bodyLarge`）。
   - 如需自定义通用文字样式，放在主题或公共组件中，不要在局部散落硬编码 `TextStyle`。

---

## 4. 代码结构与命名规范

- 页面级 Composable 统一使用 `XXXScreen` 命名，例如：`ConnectionScreen`、`FileListScreen`。
- 对应的业务逻辑放在 `XXXViewModel` 中，并通过 `StateFlow` / `UiState` 暴露状态。
- Composable 获取状态时优先使用 `collectAsStateWithLifecycle()` 等具备生命周期感知的 API。
- 工具类、通用逻辑放在 `util` / `data` 等专用包中，避免业务逻辑散落在 UI 包里。
- 文件名与类名保持一致，使用大驼峰命名，例如：`DownloadHistoryViewModel.kt`。

---

## 5. Agent（AI 助手）协作约定

- Agent 在本仓库中进行修改或新增代码时，应遵守：
  - 优先沿用现有架构和模式，不进行大规模重构，除非有明确说明。
  - 所有新增注释、日志文案和用户可见文案统一使用简体中文。
  - 遇到不确定需求时，优先以“新增可选能力、保持原有行为不变”为原则。
- Agent 生成 UI 相关代码时必须自查：
  - 是否使用统一主题颜色，而非硬编码颜色值。
  - 是否兼容浅色 / 深色主题。
  - 是否处理了空数据 / 错误 / 加载中等基本状态。

---

## 6. 禁止事项

- 禁止随意引入新的第三方 UI 库，除非有明确需求并在说明中写清原因。
- 禁止在 Composable 中直接执行耗时操作或阻塞 IO（网络、磁盘等），必须放到 ViewModel + 协程中。
- 禁止在 Activity 中堆积业务逻辑，Activity 只负责初始化、导航与顶层容器。

---

如需调整本文件中的规则，请在提交前一并更新本文件，并在提交信息或 PR 描述中说明变更原因。
